var expect = rquire('expect'),
    TodoAPI = require('TodoAPI');

// start test
describe('TodoAPI', () => {
  it('should exist', () => {
    expect(TodoAPI).toExist();
  });
  //
  describe('setTodos', () => {

  });
  //
  describe('getTodos', () => {

  });
});
